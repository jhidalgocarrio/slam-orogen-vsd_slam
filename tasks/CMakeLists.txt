# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(visual_stereoTaskLib)
ADD_LIBRARY(${VISUAL_STEREO_TASKLIB_NAME} SHARED 
    ${VISUAL_STEREO_TASKLIB_SOURCES})
add_dependencies(${VISUAL_STEREO_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${VISUAL_STEREO_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${VISUAL_STEREO_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${VISUAL_STEREO_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${VISUAL_STEREO_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${VISUAL_STEREO_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${VISUAL_STEREO_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${VISUAL_STEREO_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${VISUAL_STEREO_TASKLIB_HEADERS}
    DESTINATION include/orocos/visual_stereo)

